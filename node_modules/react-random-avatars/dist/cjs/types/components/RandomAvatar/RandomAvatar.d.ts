/// <reference types="react" />
type AvatarMode = "random" | "pattern" | "colors";
interface RandomAvatarProps {
    size?: number;
    square?: boolean;
    name?: string;
    mode?: AvatarMode;
    pattern?: string[][];
}
declare const RandomAvatar: ({ size, square, name, pattern, mode, }: RandomAvatarProps) => JSX.Element;
export default RandomAvatar;
